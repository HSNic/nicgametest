# 同步紀錄:原作者新版 20260707

同步日期:2026-07-07  
來源資料夾:`/Users/hsienchenglin/Desktop/Lineage/原作者新版20260707`  
目標資料夾:`/Users/hsienchenglin/Desktop/Lineage/idle-lineage-class-20260706-2048`  
同步前備份:`/Users/hsienchenglin/Desktop/Lineage/備份/加掛版同步前_20260707-112519`

## 本次做了什麼

| 項目 | 結果 |
|---|---|
| 同步原作者 `index.html` | 已完成 |
| 同步原作者 `js/` | 已完成 |
| 同步原作者 `css/` | 已完成 |
| 同步原作者 `assets/` | 已完成 |
| 同步原作者 `public/` | 已完成 |
| 保留 `afk-*.js` 外掛 | 已保留 21 支 |
| 重建 `assets-manifest.json` | 已完成,20059 筆 |
| 重建 `anim-manifest.json` | 已完成,425 組 |
| 更新 `last-sync.json` | 已完成,來源標記為 `原作者新版20260707` |
| 更新 `sw.js` / `version.json` | 已完成 |

## 版本資訊

| 檔案 | 目前值 |
|---|---|
| `version.json` | `{"code":"code-0062118f3343","build":"0707-1126"}` |
| `last-sync.json` | `{"syncedAt":"2026-07-07T03:26:37.671Z","source":"原作者新版20260707"}` |

## 驗證結果

| 檢查 | 結果 |
|---|---|
| `index.html` 引用檔案存在性 | 通過,50 個引用,0 個缺檔 |
| 外掛 script 數量 | 通過,21 支 |
| 原作者 JS 數量 | 通過,21 支 |
| CSS 數量 | 通過,3 個 |
| 主要外掛語法檢查 | 通過:`afk-dex.js`、`afk-wiki.js`、`afk-mobile.js`、`afk-offline.js`、`afk-fixes.js` |
| `scripts/sync-local-upstream.mjs` 語法檢查 | 通過 |
| Playwright smoke test | 未完成:本機缺 Playwright Chromium 執行檔 |
| 本機 HTTP curl 檢查 | 未完成:sandbox 內連不到已啟動的本機伺服器 |

## 注意事項

- 本次新增 `scripts/sync-local-upstream.mjs`,用途是以本機資料夾作為原作者新版來源進行同步。
- 第一次執行同步時曾發現腳本抓外掛區塊過度貪心,已修正後重新同步;目前 `index.html` 引用清單已正常。
- 尚未完成真實瀏覽器畫面驗證。正式發布前,建議用 Mac 終端機在目標資料夾跑:

```bash
python3 -m http.server 8000
```

再用瀏覽器打開:

```text
http://localhost:8000/
```

至少檢查首頁、建立/載入角色、戰鬥、手機版、掉落查詢、小百科與存檔匯出匯入。
