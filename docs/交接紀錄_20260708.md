# 專案交接紀錄（2026-07-08）

給下一個 Claude Code session 接手用，快速搞清楚目前狀況。

## 目前工作資料夾

`Lineage/加掛版/idle-lineage-class-20260707-2053-003`

（資料夾名稱會隨每次修改改變，流水號累加，見改名規則。如果實際看到的資料夾名稱不一樣，以 `ls` 結果 / 使用者告知為準，不要假設這份文件的名字永遠正確。）

## 目前狀態（2026-07-08 確認）

- git 狀態乾淨，沒有未提交的變更。
- 最新 commit：`d6f1ea93 docs: 新增發布 GitHub Release 流程文件`
- 已成功 push 到 GitHub（`git@github.com:HSNic/nicgametest.git`，`main` 分支），GitHub Pages 自動部署上線於 `https://hsnic.github.io/nicgametest/`。
- `version.json` 目前 build 為 `0707-2046`，這個版本已建立 tag `v0707-2046` 並發布過一次 GitHub Release。
- 上一批完成的工作：道具子分類篩選、共用倉庫搜尋欄、自動販賣 bug 修復（三項 UI 優化）、外掛更新套用、跑馬燈版本號動態化、原作者本體同步兩次（v3.0.108 → v3.0.114）。

## 注意：可能有新的上游更新在等

`Lineage/加掛版/` 底下目前有一個 `原作者新版20260708` 資料夾 —— 這代表原作者可能又出新版了，但**這次對話還沒處理**，只是被放進資料夾，尚未執行同步。如果使用者要接著做同步，記得走既有流程（`scripts/sync-local-upstream.mjs`，分支 → 備份 → 同步 → smoke test → 使用者實測 → 合併）。

## 重要規則（務必遵守，不用使用者提醒）

1. **只能外掛，不改本體**：所有新功能一律用 monkey-patch（`afk-*.js`）或 CSS 覆寫實作，絕不直接改 `index.html` / `js/*.js`（非 afk 開頭）/ `css/style.css`。
2. **每次改完都要**：
   - 更新 `docs/版本異動紀錄_玩家版.md` 和 `docs/版本異動紀錄_技術版.md`（各自新增一筆，加在最上面）。
   - 確認 `scripts/sync-upstream.mjs` 的 `PLUGINS` 陣列、`scripts/smoke-hooks.mjs` 的檢查清單有沒有漏掉新外掛。
   - 詢問是否要依規則重新命名工作資料夾（小版本：改完程式碼後；大版本：同步完原作者新版後，流水號 +1）。
3. **密碼/push/建 Release 網頁操作一律使用者自己做**，Claude Code 不經手密碼，只能做本機 git 操作（commit、建 tag）。
4. **大量或難回復的操作（批次 git 操作、資料夾改名、push）前一律先詢問確認**。
5. 使用者是程式小白，說明要用白話文，避免不解釋的術語。

## 相關文件位置

- 發布 Release 完整流程：`docs/發布Release流程.md`
- 外掛依賴矩陣：`docs/外掛依賴矩陣_20260706.csv`
- 版本異動紀錄（玩家版/技術版）：`docs/版本異動紀錄_玩家版.md`、`docs/版本異動紀錄_技術版.md`

## 已知但先不處理的小問題

- 角色裝備「影子長靴」缺少 `assets/anim/影子長靴/` 動畫圖檔，純視覺缺圖、不影響戰鬥，是原本就存在的既有問題，使用者表示不急。
