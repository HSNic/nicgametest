# 同步差異分析:v3.0.114 → v3.1.9(2026-07-08)

> 補記:這份記錄檔是在新規則(見 `CLAUDE.md`「同步順序倒過來」一節)訂立**之後**補寫的——本次同步在規則訂立前已套用到分支 `sync-upstream-v3.1.9`(尚未合併進 main),流程上沒有完全走到「先分析、經同意才套用」;往後每次同步都要在套用前就把這份記錄寫好、給使用者看過同意。

來源:使用者放進 `加掛版/原作者新版20260708/` 的本機新版資料夾。
比對方式:`git diff` 目前 repo 於同步前的 commit(`028daa7d`)與套用後的內容。

## 一、新增功能

- **遺物收集冊**(新檔 `js/21-relic-book.js` + `index.html` 新增 `#relic-book` 彈窗與收藏面板第四分頁「🏺 遺物」):獨立圖鑑,分類沿用裝備圖鑑 27 類,只收 `relic:true` 的物品,資料存 `player.relicDex`(共用桶 `RELICDEX_KEY`,跟裝備/道具圖鑑同一套多開合併機制)。**跟既有「聖地遺物」`mat_holy_relic`(拉斯塔巴德武器製作材料)完全無關**,是全新系統,注意別混淆。
- **114~117 件「遺物」新道具**(`js/00-data.js`,分 5 批 v3.1.0~v3.1.6):每件都綁定「單一指定怪物」掉落、機率固定 0.0001%(十萬分之一);`noEnhance:true` 無法強化、`gachaWeight:0` 永不進潘朵拉黑市、無法被祝福/賦予屬性/套席琳套裝;各自帶獨特效果(機率暈眩/冰凍/麻痺/沉默、即死 proc、固定額外傷害、受擊反傷、受擊自癒、移動速度改變重生延遲等)。
- **新裝備欄位「脛甲」(shin)**:盔甲下方新增一格防具槽,`player.eq`、負重清單、裝備視窗座標、裝備圖鑑分類、存檔遷移全部同步加入。
- **背包/裝備欄 hover 提示改版**:原本用瀏覽器原生 `title`(停留才跳出),改成即時 tooltip(`data-tip-uid`),背包與已裝備物品都適用。

## 二、數值/機制調整(改既有邏輯,非新增)

- **騎士貫穿系武器全面加傷**:巴迪須(4/6→10/12)、柴刀(4/6→10/17→11 校正)、貝卡合金(11/11→17/17)、法丘(10/10→16/16,且改雙手、穿透 40%→80%)、闊矛(6/10→12/16)、吉薩(6/8→12/14)、戟(10/6→16/12)、露西錘(9/11→15/17)。
- `petGearBonus()` 擴充:掃全部裝備欄加總 `petDmgAll`/`petHitAll`(供遺物如「牧神的放牧棍」使用)。
- `checkCanEquip`/`doEnhance`/`doBianAttr`/`pledgeBonusDrop`/`sherineSetEligible` 都加了「若是遺物則走特例」判定;潘朵拉抽獎池初始化尾端統一 for 迴圈把所有遺物權重強制清 0(雙重保險)。

## 三、新增內容範例

新增約 117~118 張全新中文檔名圖示(`assets/icons/{weapons,armors,accessories}/`),對應上述遺物,例如「三頭犬魔杖」「七彩鸚鵡喙」「白螞蛋殼」「巨熊的生命力」「歐姆的腳鐐」等;另有一件非遺物防具「雪人手套」(0.01% 掉落)。

## 四、外掛結構影響評估

- **`node scripts/smoke-hooks.mjs` 全過**:全部 `afk-*.js` 掛點 OK、掉落查詢地圖名全部已翻譯——代表這次改版沒有動到我們現有外掛依賴的 DOM id/class/全域函式名稱。
- 讀過的 diff 裡,新增的內容(遺物系統)都是**新增 DOM/新增資料**,沒有移除或改名既有的 `#collection-panel`、`#relic-book` 之外的既有面板結構,對現有外掛(`afk-dex.js`/`afk-wiki.js`/`afk-item-subfilter.js` 等)風險低。
- **已排除疑慮**:`js/10-ui-tabs.js` 新增 hover tooltip(`title` → `data-tip-uid`,一般背包分頁用)。原本擔心跟 `afk-warehouse-search.js`(讀 `data-tip-uid` 做文字篩選)衝突,實際比對後確認:`afk-warehouse-search.js` 真正依賴的是 `js/12-npc-quests.js`(`renderWarehouseNPC`)裡的 `data-tip-uid`,這個**同步前就已存在、本次未變動**(`data-tip-uid` 出現次數同步前後都是 2 次);`js/10-ui-tabs.js` 的新增是一般背包分頁另外加的,兩者互不影響。
- **小百科內容未跟進**:遺物系統、脛甲欄位、武器加傷數值都是新機制/新內容,`afk-wiki.js`/`afk-dex.js` 目前**沒有**收錄「遺物」相關頁面或搜尋索引(遺物道具目前不在 `buildItemIndex` 收錄條件內,搜尋查不到)。這屬於功能性缺口而非破壞性風險,留給下一輪 `/update-wiki` 處理;`wiki-checkpoint.json` 尚未前進。

## 五、結論

本次改動風險判定:**低**(全屬新增/數值調整,沒有 DOM 改名,smoke test 全過,截圖檢查正常)。功能驗收(建立/載入角色、戰鬥 tick、存檔匯出匯入、掉落查詢、小百科、新遺物收集冊面板、未載入角色不誤呼叫 saveGame)8 項全數 PASS,唯一的疑慮點(`data-tip-uid` 與倉庫搜尋)也已排除。**待使用者確認同意後,才合併進 main**。
