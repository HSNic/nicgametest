# 版本異動紀錄(技術版)

給之後維護者看的技術細節與測試紀錄,對應 `版本異動紀錄_玩家版.md` 的同一批改動。

**維護方式(往後每次改動都照此格式新增一筆,寫在最上面、新的在前):**

```
## <日期> build <version.json 的 build 值>(commit `<簡短 hash>`)

**類型:** 小版本(外掛/樣式調整) 或 大版本(同步原作者新版)

**改了哪些檔案、為什麼:**
- 條列

**測試結果:**
- smoke test 結果、手動測試結果
```

---

## 2026-07-07 build 0707-1623(commit `e8ac9d84`)

**類型:** 小版本(外掛/樣式調整)

**改了哪些檔案、為什麼:**
- `afk-classic-list.js`:`.classic-inventory-viewport>.list-item` 背景由 `#12141a` 調成 `#262b36`(使用者反映原本太深)。
- `afk-warehouse-skill.js`:新增 `cannotUse(item)`——裝備類呼叫原作者 `checkCanEquip()`,技能書類比照 `js/10-ui-tabs.js` 道具分頁「〔無法學習〕」判斷(`skillReqLv` 回傳 `undefined` 且尚未學過);符合則加 `afk-wh-cannot-use` class(淡紅色外框),不影響原本「(可學)」綠字標籤邏輯。
- `index.html` bump `afk-classic-list.js`、`afk-warehouse-skill.js` 版本號;`sw.js`/`version.json` 由 `stamp-sw-version.mjs` 自動更新。

**測試結果:**
- `node scripts/smoke-hooks.mjs`:15 支外掛全過。
- 使用者實機測試確認背景變淺、共用倉庫外框正常顯示。

---

## 2026-07-07(commit `dad7999d`)

**類型:** 大版本(同步原作者新版)

**改了哪些檔案、為什麼:**
- 來源:`加掛版/原作者新版20260707` 資料夾,使用 `scripts/sync-local-upstream.mjs` 同步。
- 更動檔案:`js/00-data.js`、`js/06-status-allies.js`、`js/09-vfx-render.js`、`js/13-shop-save.js`、`css/style.css`,以及 `index.html` 對應的快取版本號。`assets-manifest.json`/`anim-manifest.json` 無變化(這次沒有圖片/動畫異動)。
- 同步前已備份至 `加掛版/備份/加掛版同步作者新版前_20260707-154520`。
- 同步在獨立分支 `update-upstream-20260707` 進行,驗證通過後才合併回 `main`。

**測試結果:**
- `node scripts/smoke-hooks.mjs`:15 支外掛全過(含加掛版新增的外掛)。
- 頁面載入無異常 JS 錯誤(唯一的 404 是既有音效檔探測行為,同步前後皆存在,非本次造成)。

---

## 2026-07-07 build 0707-1530(commit `32d17994`)

**類型:** 小版本(新增外掛)

**改了哪些檔案、為什麼:**
- 新增 `afk-classic-list.js`:純 CSS 覆寫,把原作者 `decorateClassicInventoryTab()` 產生的方格背包版面(`.classic-inventory-*`)改回橫列式清單,不修改 `js/10-ui-tabs.js` 本體、不動任何函式或事件。
- 擴充既有外掛 `afk-warehouse-skill.js`:原本只判斷 `#wh-store-list`(倉庫側),新增 `#wh-inv-list`(背包側)同樣的可學判斷。
- `index.html` 掛載新外掛;`scripts/smoke-hooks.mjs` 加入 `[AFK-classic-list]` 驗證;`scripts/sync-upstream.mjs` 補登記新外掛(避免下次自動同步時被覆蓋沖掉);`docs/外掛依賴矩陣_20260706.csv` 補上這支與先前遺漏的 `afk-warehouse-skill.js`。

**測試結果:**
- `node scripts/smoke-hooks.mjs`:15 支外掛全過。
- 使用者實機測試確認橫列式外觀、共用倉庫可學標示正常。

---

## 2026-07-07 build 0707-1242(commit `0423058d`)

**類型:** 大版本(接手基準)

**改了哪些檔案、為什麼:**
- 在 `idle-lineage-class-20260706-2048`(現已改名為 `idle-lineage-class-20260707-1631-001`)執行 `git init` + `git add -A` + `git commit`,`assets/` 大型資源整份納入版控(使用者決定,未使用 Git LFS)。
- `.gitignore` 維持原樣(排除 `node_modules/`、`.wrangler/`、`.scratch/`、系統雜物檔)。

**測試結果:**
- `node scripts/smoke-hooks.mjs`:14 支外掛全過。
